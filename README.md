# BBC Good Food RSS Reader

A small Streamlit app that reads:

`https://www.bbcgoodfood.com/feed`

## Run locally

```bash
python -m pip install -r requirements.txt
streamlit run app.py
```

The reader includes article search, a display limit, cached feed results for
five minutes, and a refresh button.

